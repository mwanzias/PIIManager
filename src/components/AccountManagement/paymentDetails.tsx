import React from "react";

// This component carries the details of the payments
const PaymentDetails: React.FC = () => {
  // this will be the component that will manage the payment details of the user
  // we need to integrate to stripe and manage the payment details of the user
  return (
    <div>
      <h1>Payment Details</h1>
      <p>This is the Payment Details component.</p>
    </div>
  );
};

export default PaymentDetails;
